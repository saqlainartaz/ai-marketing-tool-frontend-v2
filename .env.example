# Client magic-link signing secret (min 16 chars). REQUIRED in production;
# a dev fallback is used locally so keyless dev/tests work.
CLIENT_LOGIN_SECRET=

# --- M1B (when Supabase project exists) ---
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# --- M2 (engine + generation) ---
# ENGINE_URL=
# ENGINE_API_KEY=
# ANTHROPIC_API_KEY=
